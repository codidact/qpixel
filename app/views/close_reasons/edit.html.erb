<% content_for :title, "Close Reasons" %>

<h1>Edit close reason</h1>

<div class="notice is-warning has-color-yellow-900 has-font-size-caption flex-row-always jc-sb ai-c">
  <i class="fas fa-exclamation-triangle"></i>
  <div>
    If you edit the description for this close reason, the change will appear on all questions that are already closed
    with this reason. <strong>Do</strong> use edits to clarify wording, but <strong>do not</strong> use them to replace
    one close reason with another. You can add a new reason and deactivate this one if you no longer want it to be
    available.
  </div>
</div>

<% if @close_reason.errors.any? %>
  <div class="notice is-danger">
    These errors prevented this close reason being saved:
    <ul>
      <% @close_reason.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= render 'form', submit_url: update_close_reason_path(global: @close_reason.community.nil? ? 1 : 0) %>
