<h1>Privileges</h1>
<p>Here you can define the reputation required to gain each available privilege. Click on a value to edit it.</p>

<%= tabs do %>
  <% tab 'Reputation', query_url(sort: 'rep'), is_active: active_search?('threshold') %>
  <% tab 'Name', query_url(sort: 'name'), is_active: active_search?('name') %>
<% end %>

<table class="table table-borderless">
  <tbody>
    <% @privileges.each do |priv| %>
      <tr>
        <td><strong><%= priv.name %></strong></td>
        <td class="js-privilege-threshold" data-name="<%= priv.name %>"><%= priv.threshold %></td>
      </tr>
    <% end %>
  </tbody>
</table>